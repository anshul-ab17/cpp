{
  "name": "codebook-lc",
  "private": true,
  "scripts": {
    "build": "bun build src/app.ts --outfile app.js --minify",
    "dev": "bun build src/app.ts --outfile app.js --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
